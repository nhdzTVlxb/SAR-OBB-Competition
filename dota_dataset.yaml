task: obb
# 数据集路径配置
path: /SAR/split  # 数据集根目录
train: images/train       # 训练图片路径 (相对于path)
val: images/val          # 验证图片路径 (相对于path)
test:                    # 测试图片路径（未使用）

# 类别定义
names:
  0: ship
  1: aircraft
  2: car
  3: tank
  4: bridge
  5: harbor

# 类别数量
nc: 6
#conda activate cypyolo13